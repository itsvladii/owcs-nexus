---
const { agent } = Astro.props;

// Role Logic & Colors
const role = agent.role.toLowerCase();
let roleColor = "text-neutral-500 bg-neutral-500/10 border-neutral-500/20";
let roleIcon = "♟";

if (role.includes("tank")) { roleColor = "text-blue-400 bg-blue-500/10 border-blue-500/20"; roleIcon = "🛡"; }
if (role.includes("dps")) { roleColor = "text-red-400 bg-red-500/10 border-red-500/20"; roleIcon = "⚔"; }
if (role.includes("support")) { roleColor = "text-emerald-400 bg-emerald-500/10 border-emerald-500/20"; roleIcon = "✚"; }
if (role.includes("coach")) { roleColor = "text-amber-400 bg-amber-500/10 border-amber-500/20"; roleIcon = "🧠"; }

const playerName = agent.player;
---

<div class="group flex items-center justify-between p-3 bg-neutral-900 border border-white/5 hover:border-white/20 hover:bg-white/[0.02] transition-all cursor-pointer">
    
    <div class="flex items-center gap-3">
        {/* Role Icon Box */}
        <div class={`w-8 h-8 flex items-center justify-center rounded border ${roleColor} text-xs`}>
            {roleIcon}
        </div>
        
        <div class="flex flex-col">
            <span class="text-sm font-bold text-white uppercase leading-none group-hover:text-amber-400 transition-colors">
                 {playerName}
            </span>
            <div class="flex items-center gap-2 mt-1">
                 <span class="text-[9px] text-neutral-500 font-mono uppercase">
                    Ex-{agent.from}
                </span>
            </div>
        </div>
    </div>

    {/* Availability Status */}
    <div class="flex items-center gap-2">
         {/* Changed hover text to Amber to match page theme */}
         <div class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-amber-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-amber-500"></span>
         </div>
    </div>

</div>